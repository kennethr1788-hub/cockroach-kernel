# PDH-3 R12 R6 GZIP PRELIGHT AUTHORIZATION

`UTC_CAPTURED`: 2026-08-03T10:28:43Z

Kenneth authorizes one new full-cardinality RunPod preflight campaign with up to
three sequential Secure Cloud L40S creation attempts, one worker at a time, no
replacement after main-bundle upload, a maximum ten-hour successful preflight
lifetime, and a twelve-dollar aggregate cost ceiling. Live pricing and
inventory must be refreshed before packet freeze. Absolute provider deadlines
must be frozen in the packet. A same-hash independent GLM 5.2 GREEN verdict is
required before the first creation request. This authorization does not permit
the measured 24-hour campaign.

The authorization covers only synthetic/sanitized data, the declared
CockroachDB full-cardinality preflight, retrieval and hash verification,
provider billing reconciliation, worker deletion, and proof of empty campaign
inventory. It does not authorize credentials or secrets in the worker, HOME or
production-state access, persistent/network volumes, public release, or any
later phase.

The prior R6 attempt remains immutable failed evidence. Its load-bearing failure
was `EVIDENCE_PROJECTION_LIMIT`; its worker was deleted and its inventory was
empty. The repaired candidate preserves one-second sampling and the hash chain
while using a gzip-compressed canonical JSON-lines evidence stream, and records
the remote semantic failure receipt when a success result is absent.

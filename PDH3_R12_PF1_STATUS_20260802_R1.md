# PDH-3 R12 PF-1 Status R1

Status: `PF1_IMPLEMENTATION_AND_SUPERVISOR_GREEN`

UTC: `2026-08-02T05:39:59Z`

Candidate commit: `3ca82bbc63e64bc8112316fae27be719799a5850`

## Direct validation

The complete local regression selection ran 136 unittest cases and returned
exit status zero. The extracted transfer bundle independently compiled 29
Python files and ran 10 smoke modules GREEN.

The R12-specific gates directly cover:

- canonical JSON receipts and record hashes;
- immutable checkpoint archives, previous-manifest chaining, local
  verification, and sequence-gap rejection;
- packet-bound host acknowledgement before the interruption canary;
- exact unprivileged user/network/PID/mount namespace construction;
- loopback-only network interface and route parsing;
- parent-death kill behavior and observer evidence ceilings;
- all mutually exclusive supervisor terminal branches: GREEN pending final
  gate, semantic failure, absent result, partial archive, corrupt archive,
  transport failure, observer loss, deadline exceeded, and teardown unproven;
- bounded SQL deadline propagation and PF-2 plan parsing;
- literal c500 three-gateway arithmetic (`167 + 167 + 166`);
- conservative growth projection;
- a preflight-only remote entry point with no measured-24-hour branch.

## Fail-closed boundary

The R12 runner cannot start the later 24-hour measured campaign. It stops after
PF-7, writes `GREEN_PENDING_PF8`, tears down its generated local CockroachDB
root, and leaves provider deletion to the independently supervised PF-8 host
control plane. Missing observer evidence, missing off-worker acknowledgement,
hash mismatch, latency failure, full scan, count/content mismatch, projection
breach, process leak, or teardown ambiguity returns nonzero.

PF-1 does not prove Linux namespace capability, provider hardware, target-scale
plans, c500 latency, 15-minute growth, worker teardown, or cost. Those remain
PF-4 through PF-8 evidence obligations.

LANE: Praxis (independent non-authoring)

PACKET_SHA256: c7de73f394151f5cc850cf085a32140e74887bf2873a37448a056966cc8f2378

VERDICT: GREEN

BLOCKERS: None.

Every kill-line item is addressed by cited, in-packet evidence:
- Silent mutation / unreceipted proposal: every serializable proposal yields exactly one receipt with a 64-character hash; malformed and unsupported-schema inputs also receive rejection receipts.
- Regression acceptance: proposal 8 (ordinary-quorum tightening that breaks a frozen success) is rejected with `REGRESSION_DETECTED` and replay evidence; it is not accepted.
- Stale-base promotion: proposal 6 rejected with `STALE_BASE_POLICY` before replay.
- Safety weakening: proposals 2–5 (ordinary/critical quorum weakening, correlation 5, correlation 1) all rejected with `SAFETY_INVARIANT_FAILED`; invariants (ordinary ≥3, critical ≥4 and ≥ ordinary, correlation ∈ [2,4]) are stated and exercised.
- Nondeterminism: five-repeat deterministic replay PASS; incident-order independence PASS; proposal-order independence PASS; two fresh-root CockroachDB trials with byte-identical per-trial state counts.
- Partial promotion: one-byte invalid receipt hash injected inside the promotion transaction; proven to leave 0 candidate policies / 0 safe proposals / 0 promotions. Promotion + receipt commit in one SERIALIZABLE transaction; invalid receipt aborts all candidate writes.
- Rollback mismatch: `build_rollback_receipt` requires exact current and prior policy hashes; explicit rollback restores only the bound prior policy; duplicate rollback forces a primary-key conflict and leaves `policy-p8-v1` golden; rollback receipt recorded in one transaction.
- Private-data egress / residue: gitleaks clean; detect-secrets only synthetic SHA-256 values; private-path scan only the deliberate fake-HOME assignment; P8 roots/worktrees/branches absent; no AWS/RunPod/Qdrant/StateV2/launchd/client-private-data/P9 surface touched.

Workflow correctness: validators exist for policy, incident, incident set, and proposal; `evaluate` has fixed failure ordering; incident set frozen by hash of canonically sorted member hashes (`dd1d0282…`); replay binds to sorted incident IDs/hashes; schema restricts policy states to `GOLDEN|SUPERSEDED` and outcomes to `PROMOTE|REJECT`; unique identities and 32-byte hash constraints enforced; explicit incident-set, promotion, and rollback tables with foreign keys; duplicate completed promotion is idempotent (proven).

Judgeability: every claim is tied to a specific artifact hash (`golden.py`, `001_golden.sql`, `run_integration.py`) and to repeatable mechanical evidence (15/15 P8 unit tests, 116/116 P3–P8 unit tests, principal test hash `14419628…`). Proposal outcomes are enumerated 1–8 with explicit reasons. Non-claims are explicit and bounded (synthetic local only; no FM training; no live mutation; timestamps are metadata).

RECUSAL_CHECK: Clear. Authoring/contributor lanes are Kimi K3/OAuth, Vibe, Devstral, and Codex. None may judge. Praxis is independent of all four and is non-authoring by charter.

NON_BLOCKING_RISKS:
- Evidence is synthetic and local; not the P9 live AWS/CockroachDB integration or a production deployment. Acknowledged in-packet as a known limitation; does not block R1 closure.
- Semantic SHA-256 recomputation lives in the deterministic application layer and is proven by the transaction harness, not by a SQL `digest()` constraint. The DB only constrains hash length/relationships. Acknowledged; acceptable because the contract defines the application layer as the authority and replay is deterministic.
- Rejected proposals are deliberately retained as audit evidence; by design, not residue.
- CockroachDB v26.2.3 is a local checksum-verified binary; cross-version behavior is out of R1 scope.

EVIDENCE_GAPS:
- No live/P9 integration evidence (out of R1 scope; explicitly non-claimed).
- No independent reproduction of the artifact hashes is possible from this lane (review-only charter); hashes are treated as untrusted evidence and judged against internal consistency and the cited mechanical results, which are self-consistent.
- No cross-distribution or cross-CockroachDB-version trials (single binary, two fresh roots).

REQUIRED_RERUNS: None for R1 closure. Determinism is covered by five-repeat replay plus incident/proposal-order independence; integration atomicity and rollback exactness are covered by two independent fresh-root trials with identical per-trial state.

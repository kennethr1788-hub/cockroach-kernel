Research current CockroachDB guidance for repairing this synthetic preflight harness. Use public web sources only. Separate documented facts from engineering inference. Do not write code, redesign the product, weaken gates, or claim launch/judge authority.

Failure A: a 50,000-task plan-analysis trial ran five semicolon-separated `INSERT ... SELECT` statements—tasks, events, two receipts per task, VECTOR(64) rows, and projections—through one `cockroach sql --execute` subprocess. The vector table already had a vector index. The subprocess exceeded 1,800 seconds. A 10,000-task trial passed. The final target remains 10k, 50k, and 100k plan-analysis scales followed by a separate 500k/5M/1M/250k full-cardinality campaign.

An existing robust seed component already supports deterministic per-table batches, `ON CONFLICT DO NOTHING`, uncertain-timeout reconciliation, exact content verification, bounded retries/deadlines, and persistent parameterized one-row vector clients. Evaluate whether the plan-analysis trial should reuse it. Specify safe batch/deadline boundaries, how exact reconciliation prevents `ON CONFLICT DO NOTHING` from hiding wrong rows, whether the vector index should remain online or be deferred during this diagnostic seed, and the canary ladder needed before a paid retry.

Failure B: a lifecycle guard failed to recognize this RunPodCTL v2.8 JSON absence envelope after successful deletion: `{"error":"failed to get pod: pod not found","code":"not_found","status":404}`. Its parser required every status/statusCode/code field to be integer 404, so string `code=not_found` defeated numeric `status=404`. Specify a narrow parser contract that accepts only a numeric 404 status plus a Pod-specific not-found message, rejects conflicting numeric statuses and unrelated/malformed 404s, and then requires empty campaign inventory before teardown GREEN.

Return: ROOT_CAUSES, MINIMAL_REPAIR, BATCH_AND_DEADLINE_CONTRACT, IDEMPOTENCY_RECONCILIATION, VECTOR_INDEX_DECISION, LIFECYCLE_404_CONTRACT, REQUIRED_TESTS, GO_NO_GO, and PRIMARY_SOURCE_LINKS.

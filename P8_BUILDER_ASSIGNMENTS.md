# P8 Builder Assignments

All inputs are synthetic and non-sensitive. No contributor may access
credentials, HOME runtime, live memory, client data, deployment, AWS, RunPod,
public actions, or later phases.

- Kimi: bounded stale-base proposal fixture in an isolated P8 worktree, with no
  authority or gate role.
- Vibe: bounded adversarial-vector generation in an isolated P8 worktree, with
  no edits or gate role.
- Devstral: sanitized one-turn CockroachDB promotion/rollback boundary review,
  with no tools, repository access, or gate role.
- Codex: policy semantics, canonical records, deterministic replay,
  promotion/rejection authority, SQL integration, reconciliation, evidence,
  and judge packet.

Every external result is untrusted. Codex accepts only findings reproduced by
local tests and records rejected or unavailable output honestly.

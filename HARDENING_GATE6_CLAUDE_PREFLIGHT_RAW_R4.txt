[claude-judge] served model: claude-opus-4-8
{
  "role": "ICARUS non-authoring evidence judge (Claude Opus 4.8, claude-judge) — process isolation, lifecycle, evidence custody, teardown, candidate immutability",
  "packet_sha256": "f1df04300bd4d865d2c0d2b87bc8c5f607a98f23e7c45d377edc84c31a04346d",
  "verdict": "GREEN",
  "blockers": [],
  "non_blocking_risks": [
    "Disclosed SSH trust-on-first-use: the wiring installs host keys from two byte-equal ssh-keyscan calls rather than a provider-signed identity, so a first-contact MITM is theoretically possible; mitigated by a scanner-clean, credential-free payload and no secrets in the measured environment.",
    "Wide fail-safe margin: expected workload is ~274s but provider stop/terminate fuses are 2026-07-28T07:50Z/08:05Z with up to 28800s paid lifetime. Cost is bounded by the $25 aggregate cap, per-hour rate ceilings, single-worker limit, and redundant lifecycle-guard plus provider-native fuses, but the gap between expected (~5 min) and worst-case (hours) runtime is large.",
    "Measured rows run as user-namespace root via unshare --user --map-root-user --net --mount-proc; egress denial (the stated threat) is separately proven, host uid stays unprivileged (10001) and DAC-confined to gate6-owned paths, and the wrapper is symmetric across all three methods, but namespace-root with a /proc mount and no explicit PID namespace is worth noting.",
    "A per-row subprocess timeout (240s) or guard/provider action failure surfaces as an uncaught exception rather than a typed CampaignError; behavior stays fail-closed (campaign aborts; provider terminate-after and the lifecycle guard still bound cost) but the abort path is less graceful than the explicit integrity checks.",
    "Disclosed construct-validity biases: the product team authored the scenarios and success rules and the completed-checkpoint cadence favors snapshot baselines. These are mandated report limitations and the design forbids composite winner scores and post-result tuning, but they remain inherent interpretation caveats."
  ],
  "evidence_gaps": [
    "The candidate comparative harness (bundle/hardening-gate5/comparative.py) and its validate_receipt are excluded from this packet (disclosed sanitization boundary due to an ephemeral CSPRNG Restic-password false positive), so I cannot directly verify receipt emission, per-trial isolation, or teardown; I rely on the pinned hash f9fa1d5c... plus an asserted Gate 5 GLM/Claude GREEN I cannot re-verify.",
    "All SHA-256 digests (packet, manifest embedded/file, 54 row hashes, tool provenance, payload, checkpoints, judge raw outputs) are builder-attested; with no tools I cannot recompute them, so their correctness is assumed. Manual checks of manifest rotation/coverage and the stop/delete epoch-to-UTC mapping did reconcile.",
    "The same-hash judge quorum for this R4 packet is not evidenced: the packet records prior R2/R3 judge-egress blocks and includes no GLM GREEN for the current hash; GLM's independent verdict over this exact packet is external to and pending relative to my review.",
    "No provider-side evidence exists yet (worker envelope match, unprivileged unshare network-denial probe result, non-measured smoke, teardown 404/empty inventory, billing state); these are future runtime obligations the design gates on, not present artifacts.",
    "The remote Python hash (d6bca2b8...) is a prior image attestation requiring mandatory remote recheck before measurement and is not yet confirmed on an actual worker.",
    "Builder identity is asserted as distinct from the non-authoring GLM/Claude reviewers but is not independently verifiable from the packet; the recusal-clear finding relies on that consistent framing plus the Gate 5 recusal-clear record."
  ],
  "recusal_check": "clear"
}

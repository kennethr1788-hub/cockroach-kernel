glm-zai: served by glm-5.2
```text
SERVED_MODEL: glm-5.2
TARGET_PACKET_SHA256: 1e3db984d08c00ee58be04573c7f5b19742a659ea162c2fcfc09d3e21981755e
TARGET_THRESHOLD_AMENDMENT_SHA256: 012716a1a377a938bc8ca20b84631b6450f407c635748929d91db59ff675e2e1
VERDICT: GREEN
CRITICAL_FINDINGS:
- NONE
HIGH_FINDINGS:
- NONE
REQUIRED_CORRECTIONS:
- NONE
RATIONALE: R3 closes both invalidated R2 staging defects with the exact scope claimed. (a) Launcher-only root ownership: section 7 binds all execution to `pdh3_r12_remote_launcher.py` as the single remote entry point and section 8 forbids the host from any worker-root pre-creation; the host's role is limited to retrieving canonical checkpoint archives and pushing signed acks into the launcher-created ack root, which preserves genuine launcher ownership rather than a nominal host-owned root. (b) Hash-bound verifier: section 7.2 mandates remote archive-hash confirmation first, isolated extraction into a new generated root, then the archive's own hash-bound verifier in mutually exclusive verify mode — no unbound helper transfer and no build path is reachable during verification. Lifecycle safety is intact: one exact Pod ID `ck-pdh3-r12-preflight-r3-01`, exactly one creation attempt, one-at-a-time worker, no replacement on any creation response, detached lifecycle guard with PPID 1 and advancing hash-chained heartbeat required before any bundle upload, PF-4 capability gate must be GREEN before any payload upload, and every terminal branch is fail-closed. Cost law is mathematically bounded: $0.50/hour × 10-hour maximum paid lifetime (07:00Z earliest creation to 17:00Z provider-native terminate, stop-after 16:45Z) plus $0.40 storage = $5.40 ≤ $6.00 ceiling, with account balance and spend limit sufficient and no billing-setting or account-limit changes. Scope is sealed against the 24-hour campaign: section 7.3 states the command graph contains no `--production` flag and no 86,400-second branch, section 7 final line forbids any 24-hour clock start, and the GREEN label is explicitly only `PDH3_R12_PREFLIGHT_GREEN__24H_RUN_REQUIRES_SEPARATE_AUTHORIZATION` — no success mislabel, no retry authority, no stage certification. Teardown is mandatory and proven: delete on success/failure/timeout/interruption, exact-ID lookup absence and empty campaign-scoped inventory required, no residual local SSH/transfer/supervisor/database/paid process permitted. The threshold-timing amendment R1 honestly discloses the prior sequencing defect (disk thresholds written after live inventory was seen) and freezes prospective thresholds before any worker creation, with explicit prohibition on changing values after a benchmark becomes visible; reduced thresholds, second benchmark, and replacement are forbidden. Section 9 kill lines cover every material failure mode including any path that would start a 24-hour campaign. This authorizes only the one bounded paid preflight; it does not certify any stage, erase the threshold-order disclosure, authorize a retry, or authorize the final 24-hour campaign.
```
